<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User — HosBank Admin</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>

    <%- include('../partials/navbar', { user: user }) %>

    <div class="container">
        <div style="margin-bottom: 1.5rem;">
            <a href="/admin/users" style="display: inline-flex; align-items: center; gap: 0.35rem; margin-bottom: 1rem;">
                ← Back to user directory
            </a>
            <h1>Edit User: <%= selectedUser.first_name %> <%= selectedUser.last_name %></h1>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger">
                <strong>Error:</strong> <%= error %>
            </div>
        <% } %>

        <div class="card" style="max-width: 600px;">
            <form action="/admin/users/<%= selectedUser.id %>/edit" method="POST">
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                    <div class="form-group">
                        <label for="firstName">First Name</label>
                        <input type="text" id="firstName" name="firstName" value="<%= selectedUser.first_name %>" required>
                    </div>

                    <div class="form-group">
                        <label for="lastName">Last Name</label>
                        <input type="text" id="lastName" name="lastName" value="<%= selectedUser.last_name %>" required>
                    </div>
                </div>

                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" value="<%= selectedUser.email %>" required>
                </div>

                <div class="form-group">
                    <label for="phone">Phone Number</label>
                    <input type="text" id="phone" name="phone" value="<%= selectedUser.phone || '' %>">
                </div>

                <div class="form-group">
                    <label for="role">Assigned System Role</label>
                    <select id="role" name="role" required>
                        <option value="CLIENT" <%= selectedUser.role === 'CLIENT' ? 'selected' : '' %>>Client</option>
                        <option value="CHARGE_CLIENT" <%= selectedUser.role === 'CHARGE_CLIENT' ? 'selected' : '' %>>Chargé Client (Manager)</option>
                        <option value="ADMIN" <%= selectedUser.role === 'ADMIN' ? 'selected' : '' %>>Administrator</option>
                    </select>
                </div>

                <div style="display: flex; gap: 1rem; align-items: center; margin-top: 1.5rem;">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <a href="/admin/users" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>

</body>
</html>